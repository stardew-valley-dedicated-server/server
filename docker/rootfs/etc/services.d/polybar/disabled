#!/bin/sh

# Polybar is always enabled so the rendering toggle button is available in VNC.
# Previously disabled when DISABLE_RENDERING=true, but the toggle button
# needs polybar to allow re-enabling rendering at runtime.

echo "false"