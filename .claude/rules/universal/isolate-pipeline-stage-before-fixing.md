# Isolate which pipeline stage introduces an artifact defect before proposing a fix

When a data artifact comes out wrong from a multi-stage pipeline (capture→segment→concat→extract→encode, or any producer→transform→consumer chain), find the stage that *first* shows the defect by probing the intermediate output at each boundary — before proposing or applying any fix. Don't fix at the stage where you *noticed* the defect, and don't let a confident-sounding mechanism story stand in for the bisection.

**Why:** A per-test recording came out 2× time-compressed. I formed a plausible mechanism ("ffmpeg defaults to 2 fps", then "duplicate frames at the segment boundary"), and proposed + applied a fix at the stage I'd been staring at (a `-r` flag on the pass-2 *extraction*) — which the user reverted as wrong. Only when I actually probed the intermediates did the real cause appear in minutes: a single-frame MPEG-TS *segment* self-reports `duration=0.5s`, and the `-f concat` *demuxer* (an earlier stage than extraction) trusts that and packs frames at half-spacing. Two cheap probes settled it — `ffprobe` on one raw segment (showed `duration=0.5`), and comparing the `concat:` protocol vs the `-f concat` demuxer on the same segments (`1/1` vs `2/1`). The mechanism story was both wrong *and* expensive: it sent the fix to the wrong layer.

**How to apply:** For any "the output artifact is wrong" bug in a chain of transforms, walk the boundaries: dump/`ffprobe`/`head` the intermediate after each stage and ask "is the defect present *here* yet?" The first stage where it appears is where to fix — often upstream of where it's visible. A reproduction that isolates one stage (probe a single input unit; swap one tool/flag and re-measure) outranks any reasoning about how the tool "should" behave. Distinct from `runtime-post-conditions-are-gates.md` (verify *outcomes* by running) and `retry-is-evidence-of-root-cause.md` (root-cause-before-bandaid, scoped to retry/fallback code): this rule is about *locating* the offending stage in a producer→consumer pipeline before you touch anything.
