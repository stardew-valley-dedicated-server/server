---
paths:
  - "mod/JunimoServer/Services/AlwaysOnServer/**"
  - "mod/JunimoServer/Services/HostAutomation/**"
  - "mod/JunimoServer/Services/Lobby/**"
  - "mod/JunimoServer/Services/CabinManager/**"
---

# Host-automation invariants

For any mod code that automates host behavior (sleep, warp, festival, save, event skip, pause), first read `decompiled/sdv-1.6.15-24356/StardewValley/Network/Dedicated/DedicatedServer.cs`. The game's own dedicated-host path usually already solves it deterministically using public entry points. Below are the concrete invariants that fall out of that approach in this codebase.

1. **`hasDedicatedHost = false` is intentional** (`AlwaysOn.cs:146-148`), not a TODO. Flipping it activates `Game1.dedicatedServer.Tick()` (every-frame from `Game1._update`) and changes cross-service behavior across ~24 services: `SaveGameMenu` player count (`SaveGameMenu.cs:184`), `Event` host routing (`Event.cs:4871, 12083`), `DialogueBox` safetyTimer (`DialogueBox.cs:462`), `FarmerTeam` semantics, `ScreenFade` alpha (`ScreenFade.cs:71-169`), `Game1.updatePause` (`Game1.cs:5394`), and farmer behavior in `Farmer.cs:1915`.

2. **Ready-check formula** for "all others ready, host is the only missing one" must be: `numberReady >= GetNumberRequired(name) - 1 && !IsReady(name) && numberReady > 0`. Used by sleep, festivalStart, festivalEnd, ready_for_save, wakeup. Maintained by `LobbyService.UpdateSleepReadyCheckExclusion` (`LobbyService.cs:818`) via `SetLocalRequiredFarmers` (`LobbyService.cs:846-848`), used in `AlwaysOnFestivals.cs:269` and `AlwaysOn.OthersReadyForSleep`, and matches `DedicatedServer.CheckOthersReady` (`DedicatedServer.cs:441-457`). Don't invent variants like `required - ready == 1` (subtraction) or exact equality — they break the lobby exclusion and Steam/LAN transports. The `numberReady > 0` guard is required because excluded unauthenticated players would otherwise trivially satisfy `ready >= required - 1`.

3. **Don't repro festival code with `/debug day`.** `Game1.whereIsTodaysFest` is populated only during a real day transition (sleep-through) from `weatherIcon`; debug-day commands skip that step. Festival paths see empty `whereIsTodaysFest` and silently no-op — any time spent reproducing after `/debug day` is wasted. To repro: set the day to one before the festival via debug, then sleep through. Festival schedule reference: [`docs/developers/testing/festivals-manual.md`](../../docs/developers/testing/festivals-manual.md).

4. **An overnight FarmEvent's completion can be draw-coupled — check where its timer advances before automating it.** `QiPlaneEvent.tickUpdate` returns `finalFadeTimer > 4000f` (`QiPlaneEvent.cs:120`), but in normal play `finalFadeTimer` advances **only in `draw()`** (`:59`, gated on `qiPlanePos.X > Viewport.Width - 480` + text fully scrolled); the one `tickUpdate` write (`:86`) is inside an `if (Escape key down)` block, dead on a headless host. So if draw isn't in lockstep with `tickUpdate`, the gate never converges, `tickUpdate` returns false forever, and clients show "Waiting for the host's event to finish." Every other common overnight FarmEvent (FairyEvent, WitchEvent, SoundInTheNightEvent, WorldChangeEvent) advances its timer in `tickUpdate` via `time.ElapsedGameTime` (draw-independent). Fix by making `tickUpdate` return true (runs the vanilla completion block), NOT by nulling `Game1.farmEvent` (strands the save — skips `makeChangesToLocation`/warp-to-bed/`showEndOfNightStuff`) or pre-seeding `sawQiPlane` (suppresses the event forever). Issue #242; fix in-code as `QiPlaneEventOverrides` + a `HandleStuckFarmEvent` watchdog.

   **Completing the host's event is necessary but not sufficient — the save barrier behind it gates the host on every connected client.** The FarmEvent itself has **no** cross-instance barrier (each instance ticks its own `farmEvent.tickUpdate` (`Game1.cs:4187`) and warps to bed independently), but the next step `showEndOfNightStuff` → `SaveGameMenu` calls `newDaySync.readyForSave()` (`SaveGameMenu.cs:105`), which blocks the host until every required online farmer signals `ready_for_save` — connected authenticated farmhands ARE required (only unauth/layout-editing excluded, `LobbyService.cs`; this is the "waiting for players (N/M)" readout, `SaveGameMenu.cs:186,208`). So a client that can't complete its own copy (e.g. a draw-gated QiPlaneEvent on a render-suppressed test client) deadlocks the day transition even after the host's event completed. So both host AND every client must complete the event — the honest client-side fix is to complete it the way a real player would (hold Escape; see `tests/test-client/GameTweaks/QiPlaneSkip.cs`), not just the host.

5. **The host's main farmhouse is internal-only/level-0 — never mirror a farmhand's cabin level onto it via the bare `HouseUpgradeLevel` setter.** Under `CabinStrategy.None` no client views the host farmhouse, so the sync serves no purpose. The `Farmer.HouseUpgradeLevel` setter (`Farmer.cs:1621`) is a bare `NetInt` write — unlike the real upgrade path (`Farmer.cs:3545-3548`) it skips `moveObjectsForHouseUpgrade`/`setMapForUpgradeLevel` and leaves `previousUpgradeLevel = -1` (default `FarmHouse.cs:52`). So when the host warps home and `setMapForUpgradeLevel` runs (level getter already bumped), the bed-migration block — gated on `if (previousUpgradeLevel >= 0)` (`FarmHouse.cs:1832`) — is **skipped**: the bed is neither converted single→double nor moved to the new `DefaultBedPosition`, lands across the exit, and soft-locks everyone (issue #346). Don't sync the host house, or drive it through the real upgrade path. Heal note: `BedFurniture.GetBedSpot()` = furniture tile + (1,1) — don't compare it against the raw `DefaultBedPosition`.

6. **Festival entry is gated on the *client's local* time inside the festival window — and a client lags server `SetTime` until the world-state broadcast lands.** A festival's "conditions" (`Data/Festivals/<season><day>`) define a window (e.g. fall27 = `Town/2200 2350`); `Game1.warpFarmer` arms `festivalStart` + opens the auto-confirming `ReadyCheckDialog` only when the warping player's **local** `timeOfDay` is in `[Open, Close]` (`Game1.cs:9956-9989`) — before Open it bounces with "Today's festival is being set up. Come back later." (`Game1.cs.2973`); after Close it warps in as a plain location. `SetTime` mutates the *host* clock, which reaches a client only on the next `NetWorldState` delta (`Multiplayer.receiveWorldState`, `:504-506`, fires `performTenMinuteClockUpdate` on the client only when the broadcast time differs). So warping a client into the festival right after `SetTime(Open)` warps it at its *stale* pre-window local time → bounce → `festivalStart` never arms → host never warps in. The fix is to **wait for the client's own state to be in-window** (poll its `/status` for `whereIsTodaysFest`/`weatherIcon==1`/`timeOfDay` in window) before warping — exactly like a real player standing in town as the clock ticks over. Do **not** "fix" this by freezing the clock or arming `festivalStart` directly: the window is real game behavior and those contrivances test a state the game forbids.

7. **`Game1.otherFarmers.Count` is NOT a "no players present" signal during an active event.** `Multiplayer.removeDisconnectedFarmers` (`Multiplayer.cs:999`) — the only code that removes a disconnected farmer from `otherFarmers` (`:1003`) — has a single caller, `UpdateEarly` (`:1823`), gated on `Game1.CurrentEvent == null` (`:1821`). `playerDisconnected` (`:971`) only *marks* the id in `disconnectingFarmers` (`:995`); it never touches `otherFarmers`. So a mid-event disconnect stays counted until the event ends — in both `otherFarmers.Count` and `getOnlineFarmers()`, whose enumerator iterates `otherFarmers.Roots` unfiltered (`FarmerCollection.cs:39,57`). Gating a festival force-end on the raw count deadlocks (event up → not removed → count stays ≥1 → never ends → event stays up). Count `getOnlineFarmers()` − host − `isDisconnecting(f)` — the *exact* predicate `DedicatedServer.Tick` uses to build its `onlineIds` set (`DedicatedServer.cs:287-293`), which the engine itself then tests as `onlineIds.Count == 0` for the festival-empty case (`:294-298`). Implemented as `AlwaysOnFestivals.CountOnlineOtherPlayers` (`AlwaysOnFestivals.cs:378`); originated from the festival no-players-end deadlock fix.

8. **The headless host can't reliably play a dialogue-heavy cutscene — end it via the engine's named idiom `endBehaviors(["End","<tag>"])`, NOT `skipEvent()` and NOT by poking menu internals.** `DialogueBox.receiveLeftClick` only advances a box after its `safetyTimer` elapses, and that timer is **750ms, not 0, because `Game1.IsDedicatedHost == false`** here (`DialogueBox.cs:367,462` — we don't use dedicated-host mode, see invariant 1). The safetyTimer and typing animation advance in `DialogueBox.update()`, which runs on the **Update path** (`Game1._update` → `updateActiveMenu`, `Game1.cs:4277/4360` — **NOT draw-coupled**), so they progress at the game tick rate even on a render-suppressed instance regardless of FPS. The slowness is *cadence + per-box pacing*, not draw-throttling: clicking per-tick still limps through a ~10-`speak` cutscene in tens of seconds (each box pays its 750ms safetyTimer + `Event.Speak`'s 500ms `timeAccumulator`, `Event.cs:171`), and `OnOneSecondUpdateTicked`-based clicking is far slower still — that handler fires every 12s at `SERVER_TPS=5`, see [`one-second-update-ticked-fires-per-game-tick.md`](one-second-update-ticked-fires-per-game-tick.md). (A per-tick `OnUpdateTicked` clicker on a render-suppressed CLIENT *does* drive a full cutscene through — see `tests/test-client/GameTweaks/WeddingCutscenePlayer.cs` — which is itself proof the box advance isn't draw-gated; it's just not the host's job.) To *complete* such an event on the host, don't fight the UI: call the engine's own end idiom. `skipEvent`'s `default` branch calls bare `endBehaviors()` with **no tag**, which skips the event's real end-behaviors (for a wedding, `endBehaviors`'s `case "wedding"` warps the spouse NPC to the couple's home + queues after-wedding dialogue, `Event.cs:4607`); the script's own `end <tag>` runs `endBehaviors(["end","<tag>"])` (via `Event.DefaultCommands.End`, `:257`) and `skipEvent` uses `endBehaviors(["End",<tag>])` for every *other* special-ending event (`:10926+`). So `endBehaviors(new[]{"End","<tag>"}, location)` reaches the identical end-state as a natural play-through (then `exitEvent()`, `:4741`, ends it cleanly) — that's the host-correct completion. This matches the mod's existing convention of driving events via named entry points (`answerDialogueAction`, `answerDialogueQuestion`, `forceEndFestival`, `hostActionNamePet`), never via `DialogueBox`/menu internals. The wedding deadlock fix uses this: `AlwaysOn.HandleWeddingEvent` readies the gate when others are ready, then `endBehaviors(["End","wedding"])`.

9. **A location-entry event that can queue more than one per day will silently NOT START the second on the host — completing event 1 is not enough, you must re-trigger `checkForEvents`; and the "already handled" guard must be keyed on the event's identity, not a bool.** Weddings are the live case (`Game1.weddingsToday` is a *list*; `getAvailableWeddingEvent` pops one per `checkForEvents` — `GameLocation.cs:15620`), but the shape is general for any queued event that fires only from `GameLocation.checkForEvents` (run by `resetForPlayerEntry`, i.e. **on location entry**, not every tick). When the host completes event 1 via `endBehaviors`, `eventFinished` warps it out and clears `eventUp`/`CurrentEvent` (`Game1.cs:7208`) — but the single post-warp `resetForPlayerEntry` *races* that teardown: it can run while `eventUp` is still true / `CurrentEvent.id` still set, so its `checkForEvents` skips the wedding branch, and then nothing re-checks. The host sits idle on a non-temp location with a queued event it never starts, and clients hang on event 2's gate forever (same freeze class as the original bug, needing two same-day events). Fix: from the per-tick automation handler, when no event is up but work is still queued and the host is on a non-temp location, re-run `Game1.currentLocation.checkForEvents()` (idempotent — vanilla's own guard no-ops if an event is already up; gate on the queue being non-empty so it costs nothing on ordinary days). Mirror vanilla's start-branch guard exactly (`!eventUp && queue.Count>0 && CurrentEvent==null && !currentLocation.IsTemporary`). **Separately**, the "I already readied+ended this ceremony" guard must be keyed on the **event's identity** (the dynamic `weddingEnd<farmerId>` gate id), NOT a bool: the host flips straight from event 1 to event 2 often *without `CurrentEvent` ever reading null* between them, so a bool latch (set on completing event 1, reset only when `CurrentEvent==null`) suppresses every event after the first. A different gate id = a new event to handle; reset the per-event timer/backstop when the gate changes. (`AlwaysOn.HandleWeddingEvent` + `StartNextQueuedWeddingIfIdle`.)

**Why:** AlwaysOn sleep refactor: hours of bandaid analysis collapsed to "the game already does this at `DedicatedServer.cs:401-467`" once we looked. The mod was reinventing ready-check + warp + answerDialogueAction with reflection and tick-modulo retries. The three invariants above each fall out of that decompiled-first reading; ignoring them in favor of subtle variants has cost real debugging cycles (silent festival no-ops, ready-check failures specific to one transport).

**How to apply:** Before proposing any new polling loop, retry heartbeat, or reflection call against a private game method, grep `decompiled/.../DedicatedServer.cs` for how `Tick()` handles the same concern. Port that pattern. When tempted to flip `hasDedicatedHost = true` to delete a workaround, treat it as a multi-service investigation, not a one-line change.
